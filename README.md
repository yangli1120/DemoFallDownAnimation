DemoFallDownAnimation
=====================

Let a view fall down from the top of the window without its parent view's layout limit at API 10, if API >= 11, maybe can use PropertyAnimation of android.
